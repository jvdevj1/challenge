<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <springProfile name="local,test">
        <property name="CONSOLE_LOG_PATTERN" value="%d %highlight(%-5level) [%blue(%t)] %yellow(%logger{70}): %msg%n"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml" />
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>
    <springProfile name="dev,pro">
        <property name="LOG_FILE" value="/var/log/baluwo/challenge.log"/>
        <property name="FILE_LOG_PATTERN" value="%d %-5level [%t] %logger{70}: %msg%n"/>
        <include resource="org/springframework/boot/logging/logback/file-appender.xml" />
        <appender name="asyncMonitoringFile" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="FILE"/>
        </appender>
        <root level="INFO">
            <appender-ref ref="asyncMonitoringFile" />
        </root>
    </springProfile>
</configuration>